(fun boot (magic_number) (do
  (var address 0x00000000)
  (var data 0)
  (when (lt address 0x80000000) (do
    (asm 'ldw a0, %m' address)
    (asm 'stw a0, %m' data)
    (if (eq data magic_number) (brk))
    (add address 512)))
  ))

(fun main (argc argv) (do
  ()

  ))
